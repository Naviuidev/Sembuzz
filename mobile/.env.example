# API base is fixed in code to https://api.sembuzz.com (see src/config/env.ts).
# Do not rely on EXPO_PUBLIC_API_URL — it is not used for the API or images.

# Frontend base for opening web routes from the app (register, legal, etc.)
EXPO_PUBLIC_FRONTEND_URL=https://sembuzz.com
